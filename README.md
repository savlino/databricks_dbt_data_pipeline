# Climbers Databricks dbt

This project loads the 8a.nu Climb Dataset CSV files into Unity Catalog Delta tables in Databricks and transforms them with dbt from raw data through staging and intermediate layers into the `climber_country_sex_stats` analytics mart.