name: climbers_databricks
version: '1.0.0'
config-version: 2

profile: climbers_databricks

model-paths: [models]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]

models:
  climbers_databricks:
    staging:
      +materialized: view
    intermediate:
      +materialized: ephemeral
    marts:
      +materialized: table
